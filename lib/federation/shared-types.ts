// Shared types for micro frontend modules
// These are exposed via module federation
// Re-exports from the main types.d.ts file

export type {
  ApiResponse,
  Category,
  BucketList,
  ViewMode,
  SortOption,
  CustomItineraryItem,
  ItineraryItem,
  Folder,
  Photo,
  ChatMessage,
} from '@/types';
